class Ternary {
    public static void main(String[] args) {
        int a=10, b=20, Greater;
        Greater = (a>b) ? a : b;
        System.out.println(Greater);

        // int a=10, b=20, c=30, Greater;
        // Greater = (a>b) ? (a>c ? a:c) : (b>c ? b:c);
        // System.out.println(Greater);
    }
}
